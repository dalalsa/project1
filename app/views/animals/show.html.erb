
      
    <div class="row">

      <div class="col s4">
       <div class="card ">
        <div class="card-content">
      
       
 <div><%= cl_image_tag  @animal.image , :size => "260x180"%></div>
 <%= @animal.title %>
 <p> <%=t(".description")%>:<%= @animal.description %></p>
 <p><%=t(".type")%>:<%= @animal.animal_type %></p>
 <p><%=t(".gender")%>:<%= @animal.gender %></p>
 <p><%=t(".city")%>:<%= @animal.city %></p>
  <p><%=t(".location")%>: <%= @animal.location %></p>
  <p><%=t(".posted_by")%> :<%=  link_to @animal.user.username ,user_path(@animal.user)%></p>
  <p><%=t(".posted_date")%>:<%= @animal.created_at.strftime "%B %d, %Y"  %></p>
      </div>
      </div>
    <% if current_user &&current_user == @animal.user%> 

          <%= button_to "حدث الحالة الى : تم التبني", "/animals/#{params[:id]}/adopted", method: :put ,class:"waves-effect waves-light btn",data: { confirm: ' هل أنت متأكد من تحويل الحالة الى: تم التبني' }%>
<br>
<%= button_to "حدث الحالة الى : تم الايجاد", "/animals/#{params[:id]}/returned", method: :put ,class:"waves-effect waves-light btn",data: { confirm: 'هل أنت متأكد من تحويل الحالة الى: تم الايجاد' }%>
<% end %>

      </div>
      
      <div class="col s8">
        <!-- Promo Content 2 goes here -->
   <section class="section comments">
    <div class="container">

        <h4 class="subtitle is-5"> </h4>
        <%#= show the user and comment content from comment.html  %>
        <%= render @animal.comments %>
        <div class="comment-form">
            <hr />
            <p class="subtitle is-3"><%=t('site.global.leave_a_reply')%></p>
             <%= render 'comments/form' %>
        </div>
    </div>
</section> 
</div> 




        

</div>
   

